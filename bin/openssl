#!/usr/bin/env node
const crypto = require("crypto");
const args = process.argv.slice(2);
if (args.includes("rand") && args.includes("-base64")) {
    const size = parseInt(args[args.length - 1]);
    console.log(crypto.randomBytes(size).toString("base64"));
}

