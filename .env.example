# Database Configuration
# Get your connection strings from: https://console.neon.tech
# Format: postgresql://[user]:[password]@[hostname]/[database]?sslmode=require

# Primary database connection (for Prisma Client queries)
DATABASE_URL="postgresql://username:password@ep-calm-glade-aglkkal.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# Direct database connection (for Prisma migrations and schema pushes)
# Note: Use the non-pooled endpoint for migrations
DIRECT_URL="postgresql://username:password@ep-calm-glade-aglkkal.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# NextAuth Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Optional: OpenAI API Key (if using AI features)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY="sk-..."

# Node Environment
NODE_ENV="development"
